# Vehicle Booking App 🚗🏍️

A full-stack vehicle booking system built with **React (Vite)** on the frontend, **Node.js + Express** for the backend, and **MySQL + Sequelize** as the database ORM.


##  📂 Project Structure

vehicle-booking-app/

│── backend/ 

│── frontend/Vite-Project
1. Navigate to backend folder:
   ```bash
   cd backend
   ```
Install dependencies:

```bash

npm install
```
Configure database:

Open config/config.json

Update your MySQL username, password, and database.

Run migrations (create tables):

```bash
npx sequelize-cli db:migrate
```
Run seeders (insert sample vehicles & types):

```bash

npx sequelize-cli db:seed:all
```
Start backend server:

```bash

node app.js

```
## frontend
Navigate to frontend folder:
```bash
cd frontend/vite-project
```


Install dependencies:
```
npm install
npm install react-router-dom @mui/material @emotion/react @emotion/styled

```


Start development server:
```
npm run dev
```